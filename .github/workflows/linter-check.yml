name: Linter check
on:
  - push
  - pull_request

jobs:
  build:
    name: Runs test
    runs-on: ubuntu-latest
    
    env:
      PROJECT_PATH: /home/runner/work/frontend-project-12
    steps:
      - name: Runs test
        run: make lint-frontend
        working-directory: ${{ env.PROJECT_PATH }}

